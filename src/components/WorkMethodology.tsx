
import HorizontalMethodologyFlow from './HorizontalMethodologyFlow';

/**
 * Work Methodology Section with Horizontal Scrollable Flow
 * - Cinematic full-screen slides with scroll-snap
 * - Ambient glows and subtle animations per step
 * - Fade-in animations for text blocks
 * - Apple-inspired minimalist design
 */
const WorkMethodology = () => {
  return <HorizontalMethodologyFlow />;
};

export default WorkMethodology;
